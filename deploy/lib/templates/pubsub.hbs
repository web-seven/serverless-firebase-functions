const functions = require('firebase-functions');
const handler = require('./{{handlerPath}}');

exports.{{functionName}} = functions.region('{{region}}').pubsub.topic('{{topic}}').onPublish(handler.{{handlerFunction}});